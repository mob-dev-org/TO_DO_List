import * as React from 'react';
import renderer from 'react-test-renderer';

import { MonoText } from '../StyledText';

it(`need to have headar `, () => {
    expect(true).toBeTruthy();
});

it(`need to have input for headar `, () => {
    expect(true).toBeTruthy();
});

it(`needs to placeholder "Write something" and save text as you type`, () => {
    expect(true).toBeTruthy();
});

it(`needs to have task after submiting inputed text`, () => {
    expect(true).toBeTruthy();
});

it(`needs to have footer with button`, () => {
    expect(true).toBeTruthy();
});

it(`needs to have "x" and "edit" button in task`, () => {
    expect(true).toBeTruthy();
});

it(`needs to have "Add" button in footer `, () => {
    expect(true).toBeTruthy();
});

// -------------------- REMOVE --------------------

it(`needs to have clear button in body`, () => {
    expect(true).toBeTruthy();
});

it(`needs to have function in clear button onPress "Clear"`, () => {
    expect(true).toBeTruthy();
});

it(`needs to have function for clearing single task with single tap on "X" button `, () => {
    expect(true).toBeTruthy();
});

// -------------------- EDIT --------------------
it(`needs to have function editing single task with edit button `, () => {
    expect(true).toBeTruthy();
});

it(`needs to change "edit" icon to "save" on click edit and oposite`, () => {
    expect(true).toBeTruthy();
});

it(`on edit press, text need to go to input, 
text is removed from todo item in list`, () => {
    expect(true).toBeTruthy();
});

it(`when you start editing change icon from 
pen to floppy disk for that specific task`, () => {
    expect(true).toBeTruthy();
});

it(`when you finish writing new text, 
by clicking on edit button save it in array,
do it by replacing existing text in item with new text`, () => {
    expect(true).toBeTruthy();
});

it(`when we click edit button to eddit task, other edit buttons disappear
`, () => {
    expect(true).toBeTruthy();
});

// -------------------- CHECK --------------------

it(`needs to mark circle button and cross the text on tap for finished tasks`, () => {
    expect(true).toBeTruthy();
});

it(`when you finish editing text it needs to have save button insted of add `, () => {
    expect(true).toBeTruthy();
});
